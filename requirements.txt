# Application dependencies (hub, personas, generator, tests).
# Trap containers (cowrie, opencanary, samba, mailhog) bring their own images.
anthropic>=0.40
PyYAML>=6.0
APScheduler>=3.10
fastapi>=0.110
uvicorn[standard]>=0.29
jinja2>=3.1
requests>=2.31
pytest>=8.0
httpx>=0.27
